---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: headscale
  name: headscale
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: headscale
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: headscale
  type: ClusterIP
