---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-mqtt
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: 10000
      protocol: TCP
      targetPort: 10000
  selector:
    app: {{ .Chart.Name }}
  type: ClusterIP
